START

1. Define patterns for different C language tokens:
      Keywords
      Identifiers
      Numbers
      Operators
      Delimiters
      Preprocessor directives
      Header files

2. Create the FLEX file with definitions, rules, and C code sections.

3. In the rules section:
      If a preprocessor directive is found:
          Print "Preprocessor Directive".

      If a header file is found:
          Print "Header File".

      If a keyword is found:
          Print "Keyword".

      If an identifier is found:
          Print "Identifier".

      If a number is found:
          Print "Number".

      If an operator is found:
          Print "Operator".

      If a delimiter is found:
          Print "Delimiter".

4. Ignore whitespace.

5. Compile the FLEX program using flex and gcc.

6. Execute the program with a C source file as input.

7. Display all recognized tokens.

STOP

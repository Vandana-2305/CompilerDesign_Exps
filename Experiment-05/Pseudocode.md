START

1. Create a FLEX file.

2. Define patterns for control keywords:
      if
      else
      for
      while
      switch
      case
      default

3. Define patterns for:
      Identifiers
      Numbers
      Braces
      Parentheses
      Colon
      Semicolon
      Relational operators
      Assignment operator

4. Return appropriate tokens to BISON.

5. Create a BISON file.

6. Define grammar rules for:
      If statement
      If-else statement
      While loop
      For loop
      Switch-case statement

7. Define condition and relational operator rules.

8. Start parsing using yyparse().

9. If the input matches the grammar:
      Display "Valid control structure syntax".

10. Otherwise:
      Display "Invalid control structure syntax".

STOP
